const getters = {
  singerId: state => state.singerId,
  songId: state => state.songId,
  albumMid: state => state.albumMid,
  keyword: state => state.keyword,
  userInfo: state => state.userInfo
};

export default getters;
